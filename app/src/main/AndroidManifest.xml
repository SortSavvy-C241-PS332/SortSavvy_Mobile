<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/thumbnail_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/thumbnail_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.SortSavvy"
        tools:targetApi="31">
        <activity
            android:name=".views.main.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".views.bottomNav.BottomNavigationActivity"
            android:exported="false"
            android:label="@string/title_activity_bottom_navigation" />
        <activity
            android:name=".views.authentication.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".views.authentication.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".views.welcome.WelcomeActivity"
            android:exported="true" />
        <activity
            android:name=".views.onboarding.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>